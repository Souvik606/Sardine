show("--- Higher-Order Functions (Returning Functions) ---")
method multiplier(factor) {
    my_func = method(n) {
        yield n * factor
    }
    yield my_func
}

times_two = multiplier(2)
times_three = multiplier(3)

show(times_two(5))
show(times_three(5))
show(times_two(10))