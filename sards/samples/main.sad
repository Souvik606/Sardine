operations = [
    1:method(a, b) {
        yield a + b
    },
    2:method(a, b) { yield a - b },
    3:method(a, b) { yield a * b },
    4:method(a, b) { yield a // b }
]

Cycle i=1:4 {
    result = operations[i](20, 5)
    show(result)
}