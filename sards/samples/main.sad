model Animal{
    attr<name>
    attr<age,gender>
    attr<weight=10,height>

    init(name,age,gender) {
        name:name,age:2*age
        gender:gender
    }

    method showName(){
        show(name)
    }

    method showAge(){
        show(age)
    }
}